# ESP8266-WEB-SERVER
A modern web-based control dashboard hosted directly on the ESP8266, enabling real-time device control over Wi-Fi without the need for external servers or cloud services. The ESP8266 acts as a standalone web server, serving a responsive and visually polished UI that can be accessed from any browser on the same network.
